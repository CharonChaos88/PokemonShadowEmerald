#include <stdio.h>
#include "global.h"
int main() {
    printf("sizeof(struct RamScriptData) = %zu\n", sizeof(struct RamScriptData));
    printf("sizeof(struct WonderCard) = %zu\n", sizeof(struct WonderCard));
    return 0;
}
